<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/card_bg">

    <EditText
        android:id="@+id/editTituloDialog"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/input_bg"
        android:hint="Título"
        android:paddingLeft="18dp"
        android:fontFamily="@font/allerta"
        android:textColor="#FFFFFF"
        android:textColorHint="#A0F0E8"
        android:textStyle="bold"/>

    <EditText
        android:id="@+id/editTipoDialog"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/input_bg"
        android:hint="Tipo"
        android:fontFamily="@font/allerta"
        android:paddingLeft="18dp"
        android:textColor="#FFFFFF"
        android:textColorHint="#A0F0E8"
        android:textStyle="bold"/>

    <EditText
        android:id="@+id/editDificuldadeDialog"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:fontFamily="@font/allerta"
        android:layout_marginBottom="10dp"
        android:background="@drawable/input_bg"
        android:paddingLeft="18dp"
        android:hint="Dificuldade"
        android:textColor="#FFFFFF"
        android:textColorHint="#A0F0E8"
        android:textStyle="bold"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end">

        <TextView
            android:id="@+id/btnCancelarDialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancelar"
            android:textColor="#FF4C4C"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="@font/allerta"
            android:padding="10dp"/>

        <TextView
            android:id="@+id/btnSalvarDialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Salvar"
            android:textStyle="bold"
            android:fontFamily="@font/allerta"
            android:textColor="#00E676"
            android:textSize="18sp"
            android:padding="10dp"/>
    </LinearLayout>

</LinearLayout>